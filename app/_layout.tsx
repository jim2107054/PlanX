import { Stack } from "expo-router";

/**
 * Renders the app's root navigation stack.
 *
 * @returns A React element containing the root `Stack` navigator.
 */
export default function RootLayout() {
  return <Stack />;
}